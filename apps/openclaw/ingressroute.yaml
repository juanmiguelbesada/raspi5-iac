apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openclaw
  namespace: openclaw
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`openclaw.juanmiguelbesada.com`)
      kind: Rule
      services:
        - name: openclaw
          port: 18789
